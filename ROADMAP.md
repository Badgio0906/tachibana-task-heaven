# タスク天国：制作・検証記録

このプロジェクトは2026-09-22のユーザー添付仕様書による独立した新作です。Ring SurvivorsのM03以降とは別工程です。

| 工程 | 依存 | 完了条件 | 状態 |
|---|---|---|---|
| T01 ゲーム本編 | 仕様全文 | 説明・練習・3ステージ・時間判定・入力・結果・再挑戦 | 完了 |
| T02 表現・音 | T01 | オリジナル図版・4音色・BGM同期・反応・日本語表示 | 完了 |
| T03 Web検証 | T01/T02 | Chromeで全編・タッチ・横縦表示・音声確認 | 完了 |
| T04 公開 | T03 | 専用GitHub、push、Pages、公開URLでプレイ確認 | 完了 |
| T05 視認性とOJT | T01〜T04 | 人物と机の比率、入力開始の明示、1拍先行OJT、PC/タッチ確認、再公開 | 完了 |

仮数値：TASK初期40、正解+2.5、MISS-6、クリア70、15MISSで失敗。判定幅は仕様どおり90/180/280ms。難易度数値は `data/settings.json` に集約。

検証証拠：`tests/artifacts/unit_integration.json`（148項目成功）、`tests/artifacts/export.log`、Chrome画面は `tests/artifacts/browser_*.png`。

完了証拠：`tests/artifacts/browser_full.json` は公開URLでの全編114 PERFECT、GOOD/OK/誤番号MISS、15MISSでの失敗、タッチ4入力、一時停止中の時計差0、実行エラー0を記録。Pages公開成功：Actions run `35719632876`。詳細と実機未検証の範囲は `VERIFICATION.md`。

## T02 表現修正：キャラクターのドット絵化

2026-09-22のユーザー指定「高密度ドットで親しみやすいがポップな、シンプルな絵」に合わせて、立花さんと上司の全12状態を96×128のオリジナルドット絵へ描き直した。状態：完了（公開更新を本コミットで実行）。依存T01/T03/T04は完了済み。輪郭をぼかさず縦横比を保つ描画に変更。タイトル・本編・クリア・失敗・スマートフォン表示を確認。譜面・音・判定は変更していない。

証拠：`tests/artifacts/pixel_characters.png`（12状態）、`tests/artifacts/pixel_v2/browser_full.json`（114 PERFECT、クリア・15MISS・タッチ4入力・実行エラー0）、同フォルダーの各画面PNG。原画の編集元は `tools/pixel_characters.py`。通常の素材再生成も新しいドット絵を出力する。

## T05 視認性とOJT

依存T01〜T04は完了済み。2026-09-23のユーザー指摘を受け、机の前面を浅い斜め俯瞰の天板に変え、立花さんを大きな前景に配置する。従来の薄緑4点表示を廃止し、各ラウンドの正確な回答開始時刻から2拍、1拍、「今！」を表示する。OJTは任意で、未処理の次ノートを1拍前から番号・画面で案内し、設定を保持する。譜面・音・判定幅・得点は維持。

完了条件：説明と一時停止からOJTを切替できること、先行案内と実際の入力時刻が一致すること、非OJTでも開始を視認できること、PC・タッチ入力と全編クリアが壊れないこと、公開URLに反映されること。検証証拠は `tests/artifacts/ojt_v1/` と `tests/artifacts/unit_integration.json`。
